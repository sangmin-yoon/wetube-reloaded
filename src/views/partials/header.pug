header 
    a(href="/").header__logo
        i.fab.fa-youtube
    nav
        ul
            li 
                a(href="/search") 
                    i.fas.fa-search
            if loggedIn
                li 
                    a(href="/videos/upload") Upload Video
                li
                    a(href="/users/edit") Edit Profile
                li
                    a(href="/users/logout") Log out!
                li 
                    a(href=`/users/${loggedInUser._id}`)
                        if loggedInUser.avataUrl === undefined
                            span 😃
                        else
                            if loggedInUser.avataUrl.includes("github")
                                img.header__avata(src=loggedInUser.avataUrl)
                            else
                                img.header__avata(src="/" + loggedInUser.avataUrl)
            else
                li 
                    a(href="/login") Login
                li 
                    a(href="/Join").header__btn Join